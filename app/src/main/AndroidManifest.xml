<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="asus.com.bwie.bwmarket">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".util.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RegisterActivity" />
        <activity
            android:name=".activity.ShowActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".activity.ParticularsActivity" />
        <activity android:name=".activity.MyInfromationActivity" />
        <activity android:name=".activity.MyCircleActivity" />
        <activity android:name=".activity.MyFootActivity" />
        <activity android:name=".activity.MyMoneyActivity" />
        <activity android:name=".activity.MyAddressActivity" />
        <activity android:name=".activity.AddAdressActivity" />
        <activity android:name=".activity.UpdataAddessActivity"></activity>
    </application>

</manifest>